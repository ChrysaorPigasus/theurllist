# Acceptance environment (.env.acc)
# Fully integrated environment - no mocks
NODE_ENV=production
BASE_URL=http://localhost:3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/theurllist_acceptance
API_VERSION=v1
TEST_TYPES=api,e2e,bdd

# Force all services to use real integrations in acceptance testing
FORCE_MOCKS=false
FORCE_INTEGRATIONS=true