# Local development environment (.env.local)
# Default settings that can be overridden for local testing
NODE_ENV=development
BASE_URL=http://localhost:3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/theurllist_local
API_VERSION=v1
TEST_TYPES=unit,api,e2e,bdd

# By default, don't force any specific mode - allow auto configuration
# Uncomment to override:
# FORCE_MOCKS=true
# FORCE_INTEGRATIONS=true

# You can also set specific services to mock or integrate
# DATABASE_INTEGRATION=mock
# API_INTEGRATION=integration
# AUTH_INTEGRATION=mock
# EXTERNAL_SERVICES_INTEGRATION=mock